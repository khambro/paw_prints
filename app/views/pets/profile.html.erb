<h1>Dashboard for <%= @pet.name %> </h1>


<h2><%= @pet.name %> Reports</h2>

<% @pet.reports.each do |r| %>
  <%= r.created_at %>
  <%= r.activity %>
  <% r.images.each do |i| %>
    <%= image_tag ("#{i.image_url}") %>
  <% end  %>
<% end  %>
