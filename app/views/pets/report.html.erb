
<div class="new-pet">

  <h1>Report Card for <%= @pet.name %> </h1>

  <%= form_tag "/report/#{@pet.id}", multipart: true do %>
    <input type="hidden" name="report[sitter_id]" value="<%= current_user.id %>" />
    <input type="hidden" name="report[pet_id]" value="<%= @pet.id %>" />
    <input type="hidden" name="report[owner_id]" value="<%= @pet.owner.id %>" />
    <br>
    <%= text_field_tag "report[activity]", nil, placeholder: "activities", class: "activities", :required => true  %>
    <br>
    <%= text_field_tag "report[emotion]", nil, placeholder: "mood", class: "mood", :required => true  %>
    <br>
    <%= label_tag "Upload Image" %>
    <%= file_field_tag "image[image_url]", :required => true  %>
    <br>
    <%= label_tag "Upload Image(optional)" %>
    <%= file_field_tag "image[image_url]" %>
    <br>
    <%= label_tag "Upload Image(optional)" %>
    <%= file_field_tag "image[image_url]" %>
    <br>
    <%= submit_tag "Send Report" %>
  <%end%>

</div>
